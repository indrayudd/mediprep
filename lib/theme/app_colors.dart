import 'package:flutter/material.dart';

/// Central palette extracted from the mockup widgets so we can keep
/// the blue gradient + muted neutrals consistent across the app.
class AppColors {
  static const primaryBlue = Color(0xFF1065E4);
  static const lightBlue = Color(0xFFE7F0FF);
  static const darkText = Color(0xFF1D1B20);
  static const mutedText = Color(0xFF49454F);
  static const outline = Color(0xFFCAC4D0);
  static const grey = Color(0xFF8C8C8C);
}
